import { DefaultFilenames } from '../const/default-filenames.const.js';
import { UploadField } from '../const/upload-field.const.js';

export const DEFAULT_STATIC_IMAGES: string[] = Object.values(DefaultFilenames);
export const STATIC_RESOURCE_FIELDS: string[] = Object.values(UploadField);
